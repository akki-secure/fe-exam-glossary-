# 基本情報技術者試験 用語集(fe-exam-glossary)

[fe-siken.com](https://www.fe-siken.com/keyword/) の用語集(シラバスVer9.2準拠)をもとに、
基本情報技術者試験の学習用にまとめている単語集です。サイトの説明文をそのまま使うのではなく、
初学者でも直感的に理解できるよう、平易な言葉と具体的な計算例に書き直しています。

## 現在の収録範囲

fe-siken.com の用語集(シラバスVer9.2)の23カテゴリすべてを収録しています(約3,450語)。
[`index.html`](./index.html) から全ページへ移動できます。各編にカード形式・穴埋め形式の2ページがあります。

- 基礎理論: 離散数学(34語)/応用数学(70語)/情報理論(62語)/通信理論(22語)/計測・制御理論(24語)
- アルゴリズムとプログラミング(144語)
- コンピュータシステム: コンピュータ構成要素(190語)/システム構成要素(89語)/ソフトウェア(172語)/ハードウェア(22語)
- 技術要素: ユーザーインタフェース(74語)/情報メディア(58語)/データベース(134語)/ネットワーク(200語)/セキュリティ(402語)
- 開発技術: システム開発技術(338語)/ソフトウェア開発管理技術(90語)
- マネジメント: プロジェクトマネジメント(159語)/サービスマネジメント(173語)/システム監査(65語)
- ストラテジ: システム戦略(94語)/システム企画(45語)/経営戦略マネジメント(115語)/技術戦略マネジメント(40語)/ビジネスインダストリ(175語)/企業活動(243語)/法務(215語)

用語名と分野構成は元サイトに合わせ、説明文・計算例はすべて学習用に独自に書き下ろしています。
基礎理論の4編(離散数学〜通信理論)は手書きHTMLで、それ以降の編は下記のスクリプトで生成しています。
「情報理論」は元サイトより1語少なくなっています。


## セキュリティ実践編(別編、523語)

基本情報技術者試験のシラバスとは別に、利用者(akki-secure)が学習用にまとめたメモ(TryHackMe等の
演習で得た知識)をもとに作成した用語集です。fe-siken.comの用語集とは無関係です。

- 偵察・情報収集(41語) / Web攻撃(50語) / 攻撃ツール・権限昇格(59語) / ネットワーク・プロトコル(103語)
- Windows/AD(52語) / macOS(19語) / デジタルフォレンジック(31語) / マルウェア解析(41語)
- 暗号・ハッシュ(17語) / ソーシャルエンジニアリング(17語) / AIセキュリティ(8語) / 標準・組織・資格(85語)

用語データは `src/data/pentest/*.txt`、生成は `python3 src/build_pentest.py` です。
ツール名や用語の意味は執筆時点の理解にもとづくもので、正確性を保証するものではありません。

## Git編(別編、59語)

利用者が学習用にまとめたGit・GitHubのメモをもとにした用語集です。用語データは `src/data/git/git.txt`、
生成は `python3 src/build_pentest.py`(ページは `git-cards.html` / `git-quiz.html`)です。

## Docker編(別編、19語)

利用者が学習用にまとめたDockerのメモをもとにした用語集です。用語データは `src/data/docker/docker.txt`、
生成は `python3 src/build_pentest.py`(ページは `docker-cards.html` / `docker-quiz.html`)です。

## 使い方(起動の仕方)

静的なHTMLなので、サーバーやインストールは不要です。

### 開く

[`index.html`](./index.html) をブラウザで開くだけです。ターミナルからは次のとおりです。

```
open index.html
```

ローカルサーバーで見たい場合は、次を実行します。

```
python3 -m http.server 8000
```

1. 実行すると `Serving HTTP on :: port 8000 ...` と表示されます。これは正常に起動した状態で、エラーではありません。
2. ブラウザのアドレス欄に http://localhost:8000 と入力すると、目次ページが表示されます。
3. 表示したままの間は、サーバーが動き続けています。このターミナルは閉じないでください。
4. ページを開くと `GET /index.html 200` のような行が増えていきます。アクセスの記録なので、気にする必要はありません。
5. 他のコマンドを打ちたいときは、新しいターミナルを開いてください。
6. 止めるときは、ターミナルで Ctrl+C を押します。止めると、ページは見られなくなります。

### 用語を編集したあとの再生成

ページは用語データから自動生成しています。データを直したら、次の順に実行してください。
Pythonの標準ライブラリだけで動くので、追加のインストールは不要です。

```
python3 src/build_pentest.py   # セキュリティ実践編・Git編・Docker編
python3 src/build.py           # 基本情報の各編と目次(index.html)
```

用語データの場所は、基本情報技術者試験が `src/data/desc/`、Git編が `src/data/git/git.txt`、Docker編が `src/data/docker/docker.txt` です。

## ファイル

各編は `<名前>-cards.html`(カード形式:用語・説明・例を読む)と `<名前>-quiz.html`(穴埋め形式:説明から用語を当てる)の2つです。
いずれもブラウザで開けます。各ページ上部のナビゲーションバーから、目次・前後の編へ移動できます。

## ソースと再生成

- `src/scrape.py` : 元サイトから分野構成と用語名だけを取得し、`src/site/structure.json` に保存します(説明文は取得しません)。
- `src/data/desc/*.txt` : 用語ごとの説明文(`用語|説明|例`)。ここを編集します。
- `src/build.py` : 上記から各編のHTMLと `index.html` を生成します。説明が未作成の用語があると `src/missing.txt` に出力して止まります。

```
python3 src/build.py
```

## 出典

用語と説明の元ネタは [基本情報技術者試験ドットコム(fe-siken.com)の用語集](https://www.fe-siken.com/keyword/) です。
本リポジトリの説明文・計算例は、そのまま転記したものではなく学習用に独自に書き直しています。
